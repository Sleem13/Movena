import { BookOpenCheck, HeartHandshake, ShieldCheck } from "lucide-react";
import { Button, Card } from "../components/common/UI.jsx";
import { PageHeader } from "../components/layout/AppShell.jsx";

export default function About({ onStart }) {
  const principles = [[ShieldCheck, "Safety before certainty", "Results stay conservative, disclose limitations, and never claim diagnosis."], [BookOpenCheck, "Transparent interpretation", "Pretrained pose models estimate landmarks; explicit rules interpret movement mechanics."], [HeartHandshake, "Built for conversation", "Reports help patients and therapists review movement together—not replace professional assessment."]];
  return <main className="mx-auto w-full max-w-7xl px-6 py-12"><PageHeader eyebrow="About the product" title="Movement insight with a clear safety boundary" description="PhysioVision AI is an engineering prototype for educational squat monitoring. It combines computer-vision landmarks, explicit biomechanics rules, and optional experimental tools." /><div className="grid gap-5 md:grid-cols-3">{principles.map(([Icon, title, text]) => <Card key={title} className="p-6"><span className="grid h-11 w-11 place-items-center rounded-xl bg-clinical-mint text-clinical-teal"><Icon size={21} aria-hidden="true" /></span><h2 className="mt-5 text-lg font-bold text-clinical-ink">{title}</h2><p className="mt-2 text-sm leading-6 text-slate-600">{text}</p></Card>)}</div><Card className="mt-6 flex flex-col gap-5 bg-gradient-to-r from-blue-50 to-teal-50 p-6 sm:flex-row sm:items-center sm:justify-between"><div><h2 className="text-xl font-bold text-clinical-ink">Ready to review a squat?</h2><p className="mt-1 text-sm text-slate-600">Use a stable, full-body recording for the clearest educational report.</p></div><Button type="button" onClick={onStart}>Open squat analyzer</Button></Card></main>;
}
