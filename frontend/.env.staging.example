# Public build-time setting. Use terraform -chdir=infra/aws output -raw application_url.
# Vercel requires this value in its matching Production/Preview environment and a rebuild.
VITE_API_BASE_URL=https://your-application.cloudfront.net
